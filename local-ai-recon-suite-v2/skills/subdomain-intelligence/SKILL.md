# Subdomain Intelligence

Review passive evidence for alternate hosts, API hosts, identity hosts, cross-host references, and previously known program hosts.

Never perform active DNS or HTTP discovery automatically.
